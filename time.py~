""" нужно создать массив, куда я ввожу дату один раз, но за каждым разом 
ввожу время: но оно в ячейки вводиться инфа по типу (чч.мм.гг + чч:мм)
и в этот же список подсчитывает время между двумя временными точками
    y = int(input("year> "))
    m = int(input("mounth> "))
    d = int(input("day> "))
"""
from datetime import *

a = [] # список куда закидываються даты
b = [] # список для записи разници между датами

j = True
while j:
    d = date(
             int(input("yearabs(> ")), 
             int(input("mounth> ")), 
             int(input("day> "))
            )
    for i in range(1, 3):        
        t = time(
                 int(input("hour> ")),
                 int(input("minute> "))
                )
         # нужно будет для отдельной работы с датами 
         a.append(datetime.combine(d, t))
    
    for i in a:
        print(i)
    
    j = input("for quit press 'q'> ")
    if j == 'q':
        j = False
    else:
        j = True

#a = []
#for i in pack:
 #   a.append(input("> "))
 
 
d = input("date> ") # тут ввести дату по всем правилам
n_a = int(input("number ask> "))

while n_a > 0:  # цикл считающий количество заявок в этот день 
    
    #тут мы вводим информацию, вкидываем ее в эксель
    #прописываем даты



    #этот блок выполняется только когда количество заявок предыдущего
    #дня исчерпалось
    if n_a == 0:
        print("выйти или изменить дату? 'quit' - 1, 'new date' - 0"):
        if 1:
            # quit
        elif 0:
            d = input("new date> ")
            n_a = input("number ask")
            
    n_a -= 1
        
        
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
    

